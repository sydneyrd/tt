namespace tt.enums
{
    public enum Strategy
    {
        BlendIn,  // AI tries to act like a human player
        CauseChaos,  // AI tries to disrupt the tasks
        ProtectSelf  // AI tries to deflect suspicion away from itself
    }
}
